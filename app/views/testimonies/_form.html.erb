<%= form_with(model: testimony, local: true) do |form| %>
  <% if testimony.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testimony.errors.count, "error") %> prohibited this testimony from being saved:</h2>

      <ul>
      <% testimony.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :depoimento %>
    <%= form.text_field :depoimento, id: :testimony_depoimento %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :testimony_student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
