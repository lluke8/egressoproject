    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
    <a class="navbar-brand" href="<%= root_path %>">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="<%= courses_path %>">Cursos <span class="sr-only">(current)</span></a>
    </div>
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="<%= students_path %>">Estudantes Egresso<span class="sr-only">(current)</span></a>
    </div>
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="<%= testimonies_path %>"> Depoimentos <span class="sr-only">(current)</span></a>
    </div>
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="<%= contacts_path %>">Contatos<span class="sr-only">(current)</span></a>
    </div>
    <div class="navbar-nav">
      <div class="dropdown">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    <%= user_session %>
     </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
     <% if user_signed_in? %>
    <a class="dropdown-item" href="#"><%= current_user.email %> - <%= link_to "Logout", destroy_user_session_path, method: :delete %></a>
    <% else %>
    <a class="dropdown-item" href="<%= new_user_session_path  %>">Login</a>
    <% end %>
    
    </div>
    </div>
    </div> 
    </div>  
    </nav>


